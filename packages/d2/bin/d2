#!/usr/bin/env node
const { makeEntryPoint, notifyOfUpdates } = require("@dhis2/cli-utils");
const command = require('..');

notifyOfUpdates(require('../package.json'));

makeEntryPoint(command).parse();